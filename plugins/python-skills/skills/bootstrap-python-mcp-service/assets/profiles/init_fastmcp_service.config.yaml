# bootstrap-python-mcp-service config profile
# Use with: scripts/init_fastmcp_service.sh --config <this-file>

name: "my-mcp-service"
mode: "project"
path: "./my-mcp-service"
python: "3.13"

# Workspace-only fields:
members: "core-lib,api-service"
profile_map: "core-lib=package,api-service=service"

force: false
initial_commit: false
no_git_init: false
