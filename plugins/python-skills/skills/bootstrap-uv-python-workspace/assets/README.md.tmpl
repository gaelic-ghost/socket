# __NAME__

## What This Is

This __TYPE__ was scaffolded with the `bootstrap-uv-python-workspace` skill.

## Quick Start

```bash
uv sync
__RUN_COMMANDS__
```

## Configuration

- Keep committed, non-secret defaults in `.env`.
- Put machine-local or secret overrides in `.env.local`.
- Generated projects use `pydantic-settings` and load `.env` first, then `.env.local`.

## Quality Checks

```bash
__TEST_COMMANDS__
```

## Notes

__NOTES__
