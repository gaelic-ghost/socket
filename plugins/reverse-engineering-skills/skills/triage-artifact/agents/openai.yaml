interface:
  display_name: "Triage Artifact"
  short_description: "Classify compiled artifacts before deeper reverse engineering."
  default_prompt: "Use $triage-artifact to classify this compiled artifact before choosing deeper reverse-engineering tools."
