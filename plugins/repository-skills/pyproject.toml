[project]
name = "repository-skills-maintenance"
version = "9.27.0"
description = "Maintainer-only Python tooling baseline for repository-skills."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=9.0.0", "mypy>=1.20.1", "PyYAML>=6.0.0", "types-PyYAML>=6.0.12.20250915"]

[tool.pytest.ini_options]
testpaths = ["skills/maintain-github-repository/tests", "skills/maintain-project-repo/tests"]
