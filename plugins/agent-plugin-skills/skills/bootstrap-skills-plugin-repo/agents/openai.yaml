interface:
  default_prompt: "Use $bootstrap-skills-plugin-repo to audit a skills-export repository first, then create or align the source-first repo structure with root `skills/`, root `.codex-plugin` packaging, repo-local discovery mirrors, maintainer docs, AGENTS guidance, Git-backed marketplace install/update guidance, and clear Codex plugin-boundary wording. Do not recreate nested staged plugin directories, manual-first local install stories, installer workflows, or install-validation workflows for this repo family."
