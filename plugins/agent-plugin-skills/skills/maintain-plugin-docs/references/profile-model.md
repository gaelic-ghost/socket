# Profile Model

This skill primarily targets skills-export repositories.

Key signals:

- canonical root `skills/`
- repo-local discovery mirrors at `.agents/skills` and `.claude/skills`
- top-level docs that explain exported skills and maintainer workflows
- root `.codex-plugin/plugin.json` packaging is allowed, but no nested staged plugin directory for the repo itself
