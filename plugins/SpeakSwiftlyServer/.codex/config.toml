#:schema https://developers.openai.com/codex/config-schema.json

# Repo-local development harness only. End-user installs should use the
# plugin-managed hooks declared in .codex-plugin/plugin.json.
#
# Experimental probe: record whatever payload Codex sends to the notify command.
notify = ["node", ".codex/hooks/notify-dump.mjs"]

[features]
codex_hooks = true
