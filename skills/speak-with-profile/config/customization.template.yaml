version: "1"

workflow:
  # Documented preference only. The current wrapper script executes local-cli.
  preferredExecutionMode: delegate
  defaultProfilesFile: config/speech/profiles.yaml
  defaultDisclosure: "This audio was generated by AI text-to-speech."
  localCli:
    defaultOutputDir: output/speech
    defaultPlaybackBackend: none
    autoplayGeneratedAudio: false
