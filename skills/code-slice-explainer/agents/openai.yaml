interface:
  display_name: "Code Slice Explainer"
  short_description: "Explain code paths, flows, and slices step by step."
  default_prompt: "Use $code-slice-explainer to explain or compare a code path from trigger to final output. Start with incoming data shape and purpose, then walk every meaningful step in order, including branches, boundaries, transformations, and a simple diagram with notes."
