# Section Schema

For this repo family, the README should emphasize:

- what the repository exports
- the hard Codex limitation from OpenAI's documented plugin model
- honest install guidance for globally installed skills
  - include a section that shows how to install the full skills set at once through [skills.sh](https://skills.sh/) with `npx skills add <owner/repo> --all`
- local authoring mirrors as local authoring mirrors, not as a packaged plugin product
- repository layout and maintainer tooling
