# Repo Maintenance Toolkit Automation Prompts

- Install the repo-maintenance toolkit into `<repo_root>` and keep the GitHub workflow wrapper enabled.
- Refresh the repo-maintenance toolkit in `<repo_root>` without deleting repo-specific custom scripts.
- Report what the repo-maintenance toolkit would install into `<repo_root>` without mutating files.
- Explain when to use `scripts/repo-maintenance/validate-all.sh`, `scripts/repo-maintenance/sync-shared.sh`, and `scripts/repo-maintenance/release.sh`.
