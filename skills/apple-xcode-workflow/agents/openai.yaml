interface:
  display_name: "Apple Xcode Workflow"
  short_description: "Guide Xcode execution, mutation safety, and fallback workflows"
  default_prompt: "Use $apple-xcode-workflow to inspect Xcode workspaces, run Apple builds or tests, note the planned rename to $xcode-app-project-workflow when helpful, and choose the right guarded execution fallback path after docs have been handled."
